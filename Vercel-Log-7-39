⏺ I found the root cause! The update-components-advanced.js script is adding 'use client' to EVERY component wrapper it
  generates. This explains why all the components ended up as client components.